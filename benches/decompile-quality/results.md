# Decompile and recovery quality (CI-gated numbers)

Measured recovery by ecosystem

measured numbers, disrobe's own except where a row names another tool; sources cited per value in this file

Every value here is produced by a committed test gate or a local measurement harness. The source field cites the exact file:line that asserts or computes it. Numbers that could not be sourced from a gate are omitted by design.

Every figure below is read verbatim from `xtask/data/recovery.json`, the same committed source the recovery SVG renders. This benchmark does not recompute or round any number; it surfaces the measured value and its gate so the README can link a single honest table. Regenerate with `cargo run -p disrobe-bench-native-unpack`.

## Headline ecosystems (Python, JVM/Dalvik, .NET, WebAssembly)

| ecosystem | metric | measured | gate / source |
|---|---|---|---|
| Python bytecode (CPython 3.14 stdlib, per-code-object recompile-equivalence) | full 574-module stdlib (representative) | 95.09% - 17378 / 18276 code objects across the entire CPython 3.14 Lib; the honest representative number (the 200-module pinned corpus over-represents recoverable modules). The module list is committed at tests/harness/full_modules_314.txt so the figure is reproducible. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py over crates/disrobe-pass-py-decompile/tests/harness/full_modules_314.txt (574 modules, 18276 code objects, CPython 3.14.5) |
| Python bytecode (CPython 3.14 stdlib, per-code-object recompile-equivalence) | 200-module pinned corpus | 96.60% - 6072 / 6286 code objects; CI floor 96.60%, pinned at the figure this corpus measures rather than a round number beneath it, so the gate has no slack to absorb a regression; a curated subset that runs higher than the full stdlib | crates/disrobe-pass-py-decompile/tests/arbitrary_recompile_gate.rs:17 (OBJECT_PCT_FLOOR 96.60); harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py over the 200-module pinned corpus; measured live 96.6 (6072 of 6286 code objects, 200 of 200 modules) on CPython 3.14.5 (a pure-or if-guard whose short-circuit exits reach an inlined terminator reassembles into a single if a or b: body, and a loop after a with recurses through the statement structurer instead of collapsing to a tuple assignment) |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.10 (161 of the pinned modules) | 91.50% - 4994 / 5458 code objects over 161 modules on CPython 3.10.20. 39 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.10.20 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.12 (177 of the pinned modules) | 95.41% - 5399 / 5659 code objects over 177 modules on CPython 3.12.13. 23 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.12.13 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.14 (all 200 pinned modules) | 96.60% - 6072 / 6286 code objects over 200 modules on CPython 3.14.5. 0 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.14.5 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.15 (199 of the pinned modules) | 96.03% - 6214 / 6471 code objects over 199 modules on CPython 3.15.0b1. 1 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.15.0b1 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| CPython legacy 1.0-3.7 (proven-correct: recompile-equivalence or structural token-match) | proven-correct | 78.50% - CI floor 150 / 191 (asserted, token-match alone holds when the 1.0-3.7 interpreter zoo is absent); 166 / 191 measured locally with the full period interpreter zoo (67 recompile-equiv + 99 token-match) | crates/disrobe-pass-py-decompile/tests/legacy_recompile.rs:31 (PROVEN_CORRECT_FLOOR 150, CI-enforced via the test job); 166/191 measured locally 2026-06-12 with python 1.0-3.12 present |
| CPython legacy 1.0-3.7 (proven-correct: recompile-equivalence or structural token-match) | proven-correct (local, full period interpreter set) | 86.91% - 166 / 191 proven correct when every period interpreter is installed (67 recompile-equivalent plus 99 structural token-match). No gate asserts this number; it depends on which 1.0-3.7 interpreters are present, so it is a local measurement and is labeled local wherever it is published. | crates/disrobe-pass-py-decompile/tests/legacy_recompile.rs run locally 2026-06-12 with python 1.0-3.12 present; the CI-asserted figure is the 150 / 191 floor in the bar above |
| WebAssembly (committed 133-fn corpus: op-coverage, 57 execution-verified under wasmtime) | op-coverage | 100.00% - 1034 of the 1034 opcodes across the 38 parseable corpus modules are lowered, where the 1034 is not disrobe's own count: wasm-tools 1.250.0 disassembled each committed .wat and its instruction inventory is pinned per fixture by BLAKE3 under crates/disrobe-pass-wasm-deob/tests/golden/external_wasm_op_inventory.json, so a decoder that stops seeing opcodes scores lower instead of shrinking its own denominator. That covers 133 of 133 functions across 38 parseable modules with every operator lowered and the emitted WAT re-assembling; the other 2 of the 40 corpus files are rejected by wasm-tools itself and are pinned with its error text, so this is 100% of the supported subset, NOT of all wasm. Lowering is not correctness: separately, 57 of 57 execution-eligible functions are EXECUTION-EQUIVALENT under wasmtime (original vs recovered: return values, trap parity, linear memory; 6 byte-identical), the remainder op-coverage-only | crates/disrobe-pass-wasm-deob/tests/external_op_denominator.rs divides 1034 lowered opcodes by the 1034 wasm-tools 1.250.0 counts over the same modules, and pins each fixture's hash so a changed corpus fails rather than re-scores; crates/disrobe-pass-wasm-deob/tests/semantic_recovery_corpus.rs measure() = 38 modules parsed / 2 skipped / 133 functions / fully_recovered==133, with the module count and function total pinned by equality and the recovered count floored at 133; tests/semantic_differential.rs = wasmtime execution differential, 57/57 execution-eligible equivalent (6 byte-identical), CI-runnable under --features sandbox; op-coverage is NOT execution-equivalence except for those 57 |
| WebAssembly (committed 133-fn corpus: op-coverage, 57 execution-verified under wasmtime) | execution-equivalence | 100.00% - 57 of 57 execution-eligible functions in the same 38 parseable modules run equivalently under wasmtime: original against recovered on the same inputs, compared on return values, trap parity, and the first 4096 bytes of linear memory, 6 of them byte-identical. Eligibility is narrower than the op-coverage population because a function needs a callable signature and no host imports to be run at all | crates/disrobe-pass-wasm-deob/tests/semantic_differential.rs, CI-runnable under --features sandbox; this is the execution-equivalence leg and it is a different, smaller population than the 1034-opcode coverage bar beside it |
| JVM classfile (EdgeCases corpus, per-method real-javac recompile) | per-method | 100.00% - CI floor 131 of 131 (every top-level method recompiles error-free) measured under real javac attribution; the pickWord switch-expression lift (9afa2306) unmasked 25 latent defects previously hidden by javac parse-masking from the __unresolved__ token, all fixed by class: pattern-switch subject resolved to its source parameter (sealed-subtype exhaustiveness), sealed/permits emitted on inner type stubs, typed default initializers on final inner-stub fields, the Exceptions attribute threaded into a real throws clause, the loop-exhausted terminal emitted after a while loop whose exit is a shared pure-return tail, loop-captured locals hoisted to a fresh final declaration, and the shared return tail of an instanceof if-else-if chain duplicated under a Region-tree fall-through analysis | crates/disrobe-pass-jvm/tests/decompile_recompile_rate.rs (report_per_method_javac_recompile asserts ok >= PER_METHOD_JAVAC_OK_FLOOR 131 of PER_METHOD_JAVAC_TOTAL 131); CI runs it via the test job with actions/setup-java (JDK 25); floor measured 2026-06-22 after fixing the pickWord-unmasked defects |
| JVM classfile (EdgeCases corpus, per-method real-javac recompile) | per-method, execution-verified | 89.31% - 117 of the same 131 top-level methods are EXECUTION-equivalent, which is a strictly stronger standard than the recompile bar beside it: the original and the recovered source are each compiled by real javac, both are run under a real JVM, and every observable per-method result is compared. Read the residual 14 as the two separate populations it is, never as one: 8 methods are javac-clean and measurably DIVERGENT, and 6 are javac-clean but not executable in isolation (5 measure a nested-type stub, 1 is not self-reproducible), so those 6 are ungraded rather than passing. The distinction is not academic in this crate, which once emitted a method that compiled cleanly and incremented a counter twice on its exception path. | crates/disrobe-pass-jvm/tests/edgecases_execution_differential.rs (EXECUTION_EQUIVALENT_FLOOR 117 of PER_METHOD_TOTAL 131, with the equivalent set pinned as a membership list of method names rather than a count, so a method cannot leave it silently and the pinned-divergent lists only ever shrink); the reference is real javac plus a real JVM comparing observable behavior, never the decompiler's own output; the_execution_differential_reports_a_double_counted_exception_path injects a second counter increment on divSafe's recovered exception path and requires this differential to report divergence while javac still accepts the file; measured 117 equivalent, 8 divergent, 6 not driven on JDK 25 |
| Dalvik recovered bodies (committed dex corpus, real JVM verifier) | verifier-clean (committed, CI) | 100.00% - 118 of 118 VERIFIER-PRESENTED classes in the committed EdgeCases/EdgeCasesKt/Hello dex pass the real JVM bytecode verifier (-Xverify:all) with 0 lifter verify failures, so the plotted figure is the verifier-presented ratio. State the other denominator too: the corpus holds 155 classes and 37 of them are link-skipped, never reaching the verifier because they reference supertypes the harness does not bundle, Kotlin's Function1 among them. That is a harness limit rather than a lifter defect, but it means 118 of 155 classes are verifier-attested and the remaining 37 are ungraded, not passing. A live-range-splitting pass recovers bodies with conflicting register types across joins, and 317 re-hosted method bodies verify clean with 0 failures, every non-stub recovered body re-hosted into an Object carrier and run through -Xverify:all. Floor 118 classes; the reference is the JVM rejecting malformed bytecode, not the lifter's own report. | crates/disrobe-pass-jvm/tests/dalvik_verifier_gate.rs (COMMITTED_VERIFY_CLEAN_CLASSES 118, COMMITTED_LIFTER_VERIFY_FAILURES 0, COMMITTED_CORPUS_CLASSES 155, COMMITTED_BODY_VERIFY_CLEAN 317, COMMITTED_BODY_VERIFY_FAILURES 0, every one an equality rather than a floor); reference = -Xverify:all over assemble_jar output, not the lifter self-report; measured 118 of 118 verifier-presented classes with 37 link-skipped and 317 re-hosted bodies clean on JDK 25, and because the corpus is committed and the translation deterministic, a figure that moves either way fails this gate until the documents state the new one |
| Dalvik recovered bodies (committed dex corpus, real JVM verifier) | body-lowering (real apks, local) | 92.50% - Two separate populations, stated separately. Self-reported body lowering: 82788 of 89516 defined methods across the three apks get a lowered body rather than a throw-stub, which is the lifter counting its own output (transmissionic 26192 of 27805 = 94.2, rustdesk 29376 of 32410 = 90.6, enrecipes 27220 of 29301 = 92.9). Verifier-attested: 2960 of 2994 recovered bodies presented to the real JVM pass -Xverify:all = 98.9 (transmissionic 980 of 993, rustdesk 1205 of 1216, enrecipes 775 of 785). The attested denominator is not the method count: 82756 non-stub candidate bodies, a deterministic 100-permille sample takes 8343, and 2994 of those can be re-hosted into an isolated Object carrier, so 5349 are excluded by harness limits and are ungraded rather than passing (1400 constructors, 1672 invokespecial receivers, 2128 unresolvable framework dependencies, 149 other). The apks are gitignored, so both figures are local; the committed-dex bar above is the CI-enforced attestation and its denominator is classes, not bodies. | crates/disrobe-pass-jvm/tests/dalvik_realworld_body_attest.rs (REAL_APK_METHOD_TOTAL 89516, SELF_REPORTED_BODIES 82788, CANDIDATE_BODIES 82756, SAMPLED_BODIES 8343, ATTESTED_PRESENTED 2994, ATTESTED_CLEAN 2960, ATTESTED_REJECTED 34, every one an equality rather than a floor, with the same counts pinned per apk in common::REAL_APKS and all 2994 verdicts pinned by name in tests/golden/dalvik_body_attest) and crates/disrobe-pass-jvm/tests/dex2jar_realworld_apks.rs (the same per-apk self-reported counts, also by equality); reference for the attested figure = java -Xverify:all rejecting malformed bytecode, not the lifter self-report |
| Detection and routing rosters (counts) | .NET protectors | 23 families | crates/disrobe-pass-dotnet/src/protectors.rs detect_all walks the 23-member Protector::ALL roster. This is a detection roster, not an aggregate claim about source or CIL recovery. Per-sample recovery evidence remains scoped to the tests that grade it. |
| Detection and routing rosters (counts) | Python source obfuscators | 20 families | crates/disrobe-pass-py-deob/src/auto_route.rs describes 20 registered source-obfuscator passes. This is a routing roster, not an aggregate source-recovery measurement. The named membership test forces this figure to move with the registered passes. |
| Detection and routing rosters (counts) | JVM / Android families | 10 families | crates/disrobe-pass-jvm/src/obfuscators.rs carries nine Protector variants, and the separate BlackObfuscator DEX path is the tenth catalogued family. This is a routing roster, not an aggregate recovered-body measurement. |
| Pickle corpus (committed fixtures graded against the CPython pickletools reference) | pickletools-graded fixtures | 100.00% - 102 of the 102 pickle fixtures committed under corpus/pickle disassemble, trace and classify in agreement with CPython's own pickletools, which is the reference this figure is graded against rather than the pass reading its own output. The denominator is pinned by equality against the committed fixture set, so a run that inspects fewer fixtures scores worse instead of shrinking what it is measured against, and the figure is a named fixture set rather than a bare count, so one fixture cannot start failing while another starts passing. | crates/disrobe-pass-pickle/tests/corpus.rs test published_pickle_corpus_bar_is_pinned_to_the_graded_fixture_set, with the_pinned_corpus_check_rejects_a_dropped_fixture_and_a_shrunken_denominator refuting both weakenings; reference = the opcode and argument tables CPython's pickletools emits, committed beside the corpus |

## Full measured set (every ecosystem in recovery.json)

| ecosystem | metric | measured | gate / source |
|---|---|---|---|
| Python bytecode (CPython 3.14 stdlib, per-code-object recompile-equivalence) | full 574-module stdlib (representative) | 95.09% - 17378 / 18276 code objects across the entire CPython 3.14 Lib; the honest representative number (the 200-module pinned corpus over-represents recoverable modules). The module list is committed at tests/harness/full_modules_314.txt so the figure is reproducible. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py over crates/disrobe-pass-py-decompile/tests/harness/full_modules_314.txt (574 modules, 18276 code objects, CPython 3.14.5) |
| Python bytecode (CPython 3.14 stdlib, per-code-object recompile-equivalence) | 200-module pinned corpus | 96.60% - 6072 / 6286 code objects; CI floor 96.60%, pinned at the figure this corpus measures rather than a round number beneath it, so the gate has no slack to absorb a regression; a curated subset that runs higher than the full stdlib | crates/disrobe-pass-py-decompile/tests/arbitrary_recompile_gate.rs:17 (OBJECT_PCT_FLOOR 96.60); harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py over the 200-module pinned corpus; measured live 96.6 (6072 of 6286 code objects, 200 of 200 modules) on CPython 3.14.5 (a pure-or if-guard whose short-circuit exits reach an inlined terminator reassembles into a single if a or b: body, and a loop after a with recurses through the statement structurer instead of collapsing to a tuple assignment) |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.10 (161 of the pinned modules) | 91.50% - 4994 / 5458 code objects over 161 modules on CPython 3.10.20. 39 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.10.20 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.12 (177 of the pinned modules) | 95.41% - 5399 / 5659 code objects over 177 modules on CPython 3.12.13. 23 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.12.13 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.14 (all 200 pinned modules) | 96.60% - 6072 / 6286 code objects over 200 modules on CPython 3.14.5. 0 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.14.5 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| Python bytecode by interpreter band (same pinned module list, each compiled and recompiled by its own interpreter) | CPython 3.15 (199 of the pinned modules) | 96.03% - 6214 / 6471 code objects over 199 modules on CPython 3.15.0b1. 1 of the 200 pinned modules do not exist in this interpreter's Lib, so the denominators differ between bands and the percentages are not a like-for-like ranking. | harness crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py run under CPython 3.15.0b1 over crates/disrobe-pass-py-decompile/tests/harness/pinned_modules_314.txt |
| CPython legacy 1.0-3.7 (proven-correct: recompile-equivalence or structural token-match) | proven-correct | 78.50% - CI floor 150 / 191 (asserted, token-match alone holds when the 1.0-3.7 interpreter zoo is absent); 166 / 191 measured locally with the full period interpreter zoo (67 recompile-equiv + 99 token-match) | crates/disrobe-pass-py-decompile/tests/legacy_recompile.rs:31 (PROVEN_CORRECT_FLOOR 150, CI-enforced via the test job); 166/191 measured locally 2026-06-12 with python 1.0-3.12 present |
| CPython legacy 1.0-3.7 (proven-correct: recompile-equivalence or structural token-match) | proven-correct (local, full period interpreter set) | 86.91% - 166 / 191 proven correct when every period interpreter is installed (67 recompile-equivalent plus 99 structural token-match). No gate asserts this number; it depends on which 1.0-3.7 interpreters are present, so it is a local measurement and is labeled local wherever it is published. | crates/disrobe-pass-py-decompile/tests/legacy_recompile.rs run locally 2026-06-12 with python 1.0-3.12 present; the CI-asserted figure is the 150 / 191 floor in the bar above |
| WebAssembly (committed 133-fn corpus: op-coverage, 57 execution-verified under wasmtime) | op-coverage | 100.00% - 1034 of the 1034 opcodes across the 38 parseable corpus modules are lowered, where the 1034 is not disrobe's own count: wasm-tools 1.250.0 disassembled each committed .wat and its instruction inventory is pinned per fixture by BLAKE3 under crates/disrobe-pass-wasm-deob/tests/golden/external_wasm_op_inventory.json, so a decoder that stops seeing opcodes scores lower instead of shrinking its own denominator. That covers 133 of 133 functions across 38 parseable modules with every operator lowered and the emitted WAT re-assembling; the other 2 of the 40 corpus files are rejected by wasm-tools itself and are pinned with its error text, so this is 100% of the supported subset, NOT of all wasm. Lowering is not correctness: separately, 57 of 57 execution-eligible functions are EXECUTION-EQUIVALENT under wasmtime (original vs recovered: return values, trap parity, linear memory; 6 byte-identical), the remainder op-coverage-only | crates/disrobe-pass-wasm-deob/tests/external_op_denominator.rs divides 1034 lowered opcodes by the 1034 wasm-tools 1.250.0 counts over the same modules, and pins each fixture's hash so a changed corpus fails rather than re-scores; crates/disrobe-pass-wasm-deob/tests/semantic_recovery_corpus.rs measure() = 38 modules parsed / 2 skipped / 133 functions / fully_recovered==133, with the module count and function total pinned by equality and the recovered count floored at 133; tests/semantic_differential.rs = wasmtime execution differential, 57/57 execution-eligible equivalent (6 byte-identical), CI-runnable under --features sandbox; op-coverage is NOT execution-equivalence except for those 57 |
| WebAssembly (committed 133-fn corpus: op-coverage, 57 execution-verified under wasmtime) | execution-equivalence | 100.00% - 57 of 57 execution-eligible functions in the same 38 parseable modules run equivalently under wasmtime: original against recovered on the same inputs, compared on return values, trap parity, and the first 4096 bytes of linear memory, 6 of them byte-identical. Eligibility is narrower than the op-coverage population because a function needs a callable signature and no host imports to be run at all | crates/disrobe-pass-wasm-deob/tests/semantic_differential.rs, CI-runnable under --features sandbox; this is the execution-equivalence leg and it is a different, smaller population than the 1034-opcode coverage bar beside it |
| JVM classfile (EdgeCases corpus, per-method real-javac recompile) | per-method | 100.00% - CI floor 131 of 131 (every top-level method recompiles error-free) measured under real javac attribution; the pickWord switch-expression lift (9afa2306) unmasked 25 latent defects previously hidden by javac parse-masking from the __unresolved__ token, all fixed by class: pattern-switch subject resolved to its source parameter (sealed-subtype exhaustiveness), sealed/permits emitted on inner type stubs, typed default initializers on final inner-stub fields, the Exceptions attribute threaded into a real throws clause, the loop-exhausted terminal emitted after a while loop whose exit is a shared pure-return tail, loop-captured locals hoisted to a fresh final declaration, and the shared return tail of an instanceof if-else-if chain duplicated under a Region-tree fall-through analysis | crates/disrobe-pass-jvm/tests/decompile_recompile_rate.rs (report_per_method_javac_recompile asserts ok >= PER_METHOD_JAVAC_OK_FLOOR 131 of PER_METHOD_JAVAC_TOTAL 131); CI runs it via the test job with actions/setup-java (JDK 25); floor measured 2026-06-22 after fixing the pickWord-unmasked defects |
| JVM classfile (EdgeCases corpus, per-method real-javac recompile) | per-method, execution-verified | 89.31% - 117 of the same 131 top-level methods are EXECUTION-equivalent, which is a strictly stronger standard than the recompile bar beside it: the original and the recovered source are each compiled by real javac, both are run under a real JVM, and every observable per-method result is compared. Read the residual 14 as the two separate populations it is, never as one: 8 methods are javac-clean and measurably DIVERGENT, and 6 are javac-clean but not executable in isolation (5 measure a nested-type stub, 1 is not self-reproducible), so those 6 are ungraded rather than passing. The distinction is not academic in this crate, which once emitted a method that compiled cleanly and incremented a counter twice on its exception path. | crates/disrobe-pass-jvm/tests/edgecases_execution_differential.rs (EXECUTION_EQUIVALENT_FLOOR 117 of PER_METHOD_TOTAL 131, with the equivalent set pinned as a membership list of method names rather than a count, so a method cannot leave it silently and the pinned-divergent lists only ever shrink); the reference is real javac plus a real JVM comparing observable behavior, never the decompiler's own output; the_execution_differential_reports_a_double_counted_exception_path injects a second counter increment on divSafe's recovered exception path and requires this differential to report divergence while javac still accepts the file; measured 117 equivalent, 8 divergent, 6 not driven on JDK 25 |
| BEAM stripped Core Erlang (committed OTP 27.3.4 recompile-execution corpus) | recompile-execution | 94.74% - Under OTP 27.3.4, 18 of the 19 explicitly named committed Erlang modules recover from BEAM with Dbgi and Docs stripped to Core Erlang source that recompiles, preserves the original export set, and returns the same observable result from the module's committed test/0 battery. This is a test/0 observation over each module, not a claim of equivalence for every possible input to every exported function. | crates/disrobe-pass-beam/tests/erlc_recompile_equivalence.rs pins the 19 module names, requires the live measured equivalent and population counts to equal this bar's raw 18 and 19, parses the raw original BEAM before stripping, proves stripped and raw exports agree, compares recompiled exports to that raw set, and compares test/0 exit status and stdout under real erl; real_erlang_runtime_rejects_a_recompiled_wrong_test_result injects a recovered test/0 that still recompiles with matching exports and requires the runtime differential to reject it; the shared Erlang helper and the Linux provisioning check in .github/workflows/ci.yml read releases/<major>/OTP_VERSION and reject any value other than 27.3.4 before DISROBE_REQUIRE_ERLANG=1 makes absence fatal |
| Go type-name recovery (go1.26.3 fixture) | type names | 100.00% - CI floor >= 85% on the committed stripped fixture; 838 of 838 (100%) measured, with the type count and the recovered count both pinned in the gate so a change to either forces a re-measure (typelinks/moduledata survive -s -w stripping) | crates/disrobe-pass-go/tests/go_typemeta.rs (typemeta_recovers_real_type_names_on_go126 asserts the type total equals 838 and the recovered count is at least 838 on common::HELLO_STRIPPED, with ratio >= 0.85 kept as a secondary guard); the stripped fixture is force-committed (git add -f, overriding the crates/disrobe-pass-go/tests/fixtures/*.exe gitignore) so the gate runs in CI; the garble/embed/generics/magic-stomp fixtures are force-committed the same way so those gates also run; 528/528 measured on go1.26.3 2026-06-12 |
| Dalvik recovered bodies (committed dex corpus, real JVM verifier) | verifier-clean (committed, CI) | 100.00% - 118 of 118 VERIFIER-PRESENTED classes in the committed EdgeCases/EdgeCasesKt/Hello dex pass the real JVM bytecode verifier (-Xverify:all) with 0 lifter verify failures, so the plotted figure is the verifier-presented ratio. State the other denominator too: the corpus holds 155 classes and 37 of them are link-skipped, never reaching the verifier because they reference supertypes the harness does not bundle, Kotlin's Function1 among them. That is a harness limit rather than a lifter defect, but it means 118 of 155 classes are verifier-attested and the remaining 37 are ungraded, not passing. A live-range-splitting pass recovers bodies with conflicting register types across joins, and 317 re-hosted method bodies verify clean with 0 failures, every non-stub recovered body re-hosted into an Object carrier and run through -Xverify:all. Floor 118 classes; the reference is the JVM rejecting malformed bytecode, not the lifter's own report. | crates/disrobe-pass-jvm/tests/dalvik_verifier_gate.rs (COMMITTED_VERIFY_CLEAN_CLASSES 118, COMMITTED_LIFTER_VERIFY_FAILURES 0, COMMITTED_CORPUS_CLASSES 155, COMMITTED_BODY_VERIFY_CLEAN 317, COMMITTED_BODY_VERIFY_FAILURES 0, every one an equality rather than a floor); reference = -Xverify:all over assemble_jar output, not the lifter self-report; measured 118 of 118 verifier-presented classes with 37 link-skipped and 317 re-hosted bodies clean on JDK 25, and because the corpus is committed and the translation deterministic, a figure that moves either way fails this gate until the documents state the new one |
| Dalvik recovered bodies (committed dex corpus, real JVM verifier) | body-lowering (real apks, local) | 92.50% - Two separate populations, stated separately. Self-reported body lowering: 82788 of 89516 defined methods across the three apks get a lowered body rather than a throw-stub, which is the lifter counting its own output (transmissionic 26192 of 27805 = 94.2, rustdesk 29376 of 32410 = 90.6, enrecipes 27220 of 29301 = 92.9). Verifier-attested: 2960 of 2994 recovered bodies presented to the real JVM pass -Xverify:all = 98.9 (transmissionic 980 of 993, rustdesk 1205 of 1216, enrecipes 775 of 785). The attested denominator is not the method count: 82756 non-stub candidate bodies, a deterministic 100-permille sample takes 8343, and 2994 of those can be re-hosted into an isolated Object carrier, so 5349 are excluded by harness limits and are ungraded rather than passing (1400 constructors, 1672 invokespecial receivers, 2128 unresolvable framework dependencies, 149 other). The apks are gitignored, so both figures are local; the committed-dex bar above is the CI-enforced attestation and its denominator is classes, not bodies. | crates/disrobe-pass-jvm/tests/dalvik_realworld_body_attest.rs (REAL_APK_METHOD_TOTAL 89516, SELF_REPORTED_BODIES 82788, CANDIDATE_BODIES 82756, SAMPLED_BODIES 8343, ATTESTED_PRESENTED 2994, ATTESTED_CLEAN 2960, ATTESTED_REJECTED 34, every one an equality rather than a floor, with the same counts pinned per apk in common::REAL_APKS and all 2994 verdicts pinned by name in tests/golden/dalvik_body_attest) and crates/disrobe-pass-jvm/tests/dex2jar_realworld_apks.rs (the same per-apk self-reported counts, also by equality); reference for the attested figure = java -Xverify:all rejecting malformed bytecode, not the lifter self-report |
| Detection and extraction breadth (counts, not percentages) | Containers | 33 extracted from a committed input / 100 detected - 100 formats detected, and each declares a payload extractor. 33 of them write member bytes from an input this repository commits, which is the measured leg. The other 67 have no committed input, so nothing here states whether their extractors work. | ContainerKind enum crates/disrobe-binfmt/src/container.rs (ContainerKind::ALL is [Self; 100], 100 real variants excluding None), asserted in detected_count_is_one_hundred; every one of the 100 declares a payload extractor rather than a metadata summary or an external-tool hand-off, asserted in every_declared_format_carries_a_payload_extractor, which is a roster declaration and not a run. The delivered count is the measured leg: 33 of the 100 are driven to member bytes on disk by an input this repository commits, measured by crates/disrobe-cli/tests/container_breadth.rs over `git ls-files`, pinned in crates/disrobe-cli/tests/golden/container_breadth.txt and read back by published_container_counts_match_this_enum. The remaining 67 carry an extractor no committed input reaches, so they are unverified rather than shown to fail: absence of an input is not evidence of a broken extractor, and nothing here claims they work. The declared superset spans archives/installers (ZIP/tar/7z/RAR/cab/MSI/NSIS/deb/rpm/Docker/OCI/ISO), bare single-stream compression (gz/bz2/zst/lzma/lzip/lz4-frame/zlib/.Z), legacy archives (ar/arj/arc/lzh/lzop/uzip/xamarin-xalz/par2/ELF-overlay carve), embedded-linux filesystems (squashfs/cramfs/ext4/romfs/minixfs/jffs2/ubi-ubifs/yaffs/erofs/ntfs/android-sparse/btrfs-send), disk-image/partition containers (GPT/MBR/VHD/VHDX/WIM walked through FAT), and vendor firmware decryptors (D-Link AES/EnGenius XOR/Autel table/QNAP PC1 plus CRC-verified Netgear/Xiaomi/Tesla carves); a recursive carve-everything engine (multi-magic scan, chunk model, depth recursion, entropy gating) drives nested extraction. InnoSetup writes the decoded setup-data block stream (per-file split via the version-specific TSetupHeader parse is the documented limit) and InstallShield writes its stored and zlib members; the arj/arc/stuffit/partclone heavy codecs are carved verbatim with a documented note |
| Detection and extraction breadth (counts, not percentages) | PyArmor samples | 72 recovered / 72 detected | crates/disrobe-pass-pyarmor/tests/static_unpack_corpus.rs (RECOVERY_FLOOR 72; recovers_real_source_from_v8_and_v9_corpus counts the committed v8 and v9 wrappers and asserts the recovered count against this pair); measured 72/72 locally 2026-06-11 |
| Detection and routing rosters (counts) | .NET protectors | 23 families | crates/disrobe-pass-dotnet/src/protectors.rs detect_all walks the 23-member Protector::ALL roster. This is a detection roster, not an aggregate claim about source or CIL recovery. Per-sample recovery evidence remains scoped to the tests that grade it. |
| Detection and routing rosters (counts) | Python source obfuscators | 20 families | crates/disrobe-pass-py-deob/src/auto_route.rs describes 20 registered source-obfuscator passes. This is a routing roster, not an aggregate source-recovery measurement. The named membership test forces this figure to move with the registered passes. |
| Detection and routing rosters (counts) | JVM / Android families | 10 families | crates/disrobe-pass-jvm/src/obfuscators.rs carries nine Protector variants, and the separate BlackObfuscator DEX path is the tenth catalogued family. This is a routing roster, not an aggregate recovered-body measurement. |
| Detection and routing rosters (counts) | Shell obfuscation modes | 19 families | The shell chain catalog carries 19 named PowerShell, bash, and batch obfuscation modes after excluding scripting formats handled alongside them. This is a routing roster, not a reversal rate; the count test pins all 19 identifiers, and a companion test records the 17 full plus 2 partial support split. |
| Obfuscator and bundler family coverage (counts) | JS bundlers | 11 families | BundlerKind enum crates/disrobe-pass-js-deob/src/bundle/mod.rs carries 12 variants; SystemJs and Amd are one published family (the unbundler treats RequireJS and AMD as the SystemJS lineage), so 12 variants render as 11 published bundlers. This entry previously recorded its own provenance as the README row it is used to validate, which made the check a copy compared against its original. |
| Obfuscator and bundler family coverage (counts) | Lua chain catalog entries | 16 families | crates/disrobe-pass-lua/src/chain_detector.rs:169 (CATALOG_COUNT = 16; 14 obfuscator entries plus Luau and GLua dialect detectors) |
| Obfuscator and bundler family coverage (counts) | Lua VM-devirt on real tool output (IronBrew2 2.7.0) | 1 family | IronBrew2 2.7.0 is the one Lua VM-devirtualization family graded on committed output from the real obfuscator. The real-Lua execution differential runs the recovered output against the original across hello, arith, control, tables, and edge samples in standard and MAX mode. |
| Obfuscator and bundler family coverage (counts) | Lua VM-devirt on an in-house sample only (MoonSec shape, no real sample) | 1 family | This in-house, not real, no real sample leg covers aztup_brew, boronide, darksec, hercules, luraph, moonsec_v1, moonsec_v2, moonsec_v3, and psu reference containers. It records shape recovery only and is not evidence that any named obfuscator's own output was reversed. |
| Obfuscator and bundler family coverage (counts) | WASM direct transformation helper families | 4 families | This is a source catalog count, not a measured claim about output from the named upstream tools. crates/disrobe-pass-wasm-deob/src/detect.rs declares exhaustive per-family support with separate direct-transformation-helper and wasm-deob-pipeline-delivery axes. crates/disrobe-pass-wasm-deob/tests/published_wasm_reverser_roster.rs derives four helper families from that catalog and pins their named membership against this bar: Jscrambler-WASM strip+fold, Wobfuscator optable lift, Tigress-via-Emscripten standalone unflatten and name helpers, and Wasmixer stub-unwrap. crates/disrobe-pass-wasm-deob/tests/obfuscators_e2e.rs exercises each direct helper on hand-authored modules. crates/disrobe-pass-wasm-deob/tests/recover_differential.rs runs the three pipeline-delivered transformations, Jscrambler-WASM, Wobfuscator, and Wasmixer, against fixture-backed recovery and wasmtime behavior differentials. Tigress-via-Emscripten helpers are standalone and not on that path. wasm-name-obfuscator is excluded from the helper count because it has neither a direct helper nor pipeline delivery and hex renames destroy the original names |
| Ruby YARV (MRI recompile-equivalence oracle) | greeter | 100.00% - real MRI recompiles the recovered source and the YARV opcode multiset matches 79 of 79 opcodes, so this fixture is exact. The plotted 100 is the floor this gate enforces, and the measured value is asserted at or above it, so a regression fails instead of quietly lowering the published number. A third committed fixture, hello.rb, also matches 4 of 4 and is not plotted separately. | crates/disrobe-pass-ruby/tests/yarv_recompile_oracle.rs:105 (greeter >= 100) |
| Ruby YARV (MRI recompile-equivalence oracle) | megafile | 98.67% - real MRI recompiles the recovered source and the YARV opcode multiset matches 23648 of 23966 opcodes, which is 98.67%. The plotted 98 is the floor this gate enforces, not the measurement, so the bar shows the guaranteed minimum and the measured value is asserted at or above it. | crates/disrobe-pass-ruby/tests/yarv_recompile_oracle.rs (megafile >= 98) |
| React Native Hermes (committed hermesc-built HBC v96 sample: op-coverage, CI-gated) | op-coverage | 100.00% - self-authored sample.js compiled with the real hermesc (HBC v96); all 8 functions lift with correct names and source-matching bodies at 0 fallback ops; CI-gated | crates/disrobe-pass-mobile/tests/real_hermes_sample.rs (hbc_v96_sample_recovers_every_function_at_full_op_coverage asserts total_fallback_ops == 0) |
| React Native Hermes production-bundle parse scale (local non-redistributable bundle, secondary, not CI-gated) | functions parsed | 122633 fns | crates/disrobe-pass-mobile/tests/real_hermes_discord.rs:112 (assert_eq function count = 122_633); local-only, fixture is gitignored |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | windows/amd64 | 94.92% - 1869 / 1969 function names recovered from the stripped build. The unstripped build of the same source recovers all 1969. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | windows/386 | 100.00% - 1994 / 1994 function names recovered from the stripped build. The unstripped build of the same source recovers all 1994. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | linux/amd64 | 93.81% - 1836 / 1957 function names recovered from the stripped build. The unstripped build of the same source recovers all 1957. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | linux/386 | 99.94% - 1978 / 1979 function names recovered from the stripped build. The unstripped build of the same source recovers all 1979. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | linux/arm64 | 94.16% - 1807 / 1919 function names recovered from the stripped build. The unstripped build of the same source recovers all 1919. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | darwin/amd64 | 88.78% - 1845 / 2078 function names recovered from the stripped build. The unstripped build of the same source recovers all 2078. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Go function-name recovery from a stripped binary (pclntab, graded against go tool nm on the same build) | darwin/arm64 | 90.01% - 1839 / 2043 function names recovered from the stripped build. The unstripped build of the same source recovers all 2043. The shortfall is assembly ABI0 entry points and platform linker symbols that carry no distinct pclntab entry, so a stripped image does not contain them to recover. | crates/disrobe-pass-go/tests/go_published_function_name_bars.rs grades the committed stripped image for this platform against the committed `go tool nm` symbol dump beside it, pinning the denominator by equality and the recovered count as a floor, and it needs no toolchain; crates/disrobe-pass-go/tests/go_cross_arch_va_recovery.rs re-derives the same names from a live build of the same source, but it returns without grading anything when go1.26.3 is not installed and its own floors are coarser than this figure, so it is a second leg rather than what holds this number |
| Secret recall on the committed planted APK (both tools read the same file) | disrobe frisk | 100.00% - 8 of 8 planted secrets recalled from corpus/recon/apk/planted-secrets.apk. The eight are named individually in this bar's membership list, so one secret cannot stop being found while another starts and leave the count at 8. The denominator is pinned by equality against the committed ground truth, so a run that inspects fewer secrets scores worse rather than shrinking what it is measured against. | benches/head-to-head/src/frisk.rs test published_planted_apk_secret_bars_are_pinned_by_membership, which scans the committed apk and asserts every named secret is recalled; the ground truth is the planted token list, not frisk's own output |
| Secret recall on the committed planted APK (both tools read the same file) | apkleaks 2.6.3 | 62.50% - 5 of the same 8 planted secrets, measured by running apkleaks 2.6.3 over the same committed apk rather than quoting its documentation. It does not recall the AWS secret access key, the HTTP Basic auth credential, or the session JWT. This bar is asserted exact in both directions: publishing more than apkleaks finds would overstate it, and publishing less would flatter disrobe. | benches/head-to-head/src/frisk.rs test published_planted_apk_secret_bars_are_pinned_by_membership, which pins the version it grades so the row and the measured build cannot drift apart |
| frisk IOC category recall on the committed planted tree | planted non-secret IOC categories | 100.00% - All 6 planted non-secret IOC categories are represented in the scan of corpus/recon/planted: endpoint, manifest, url, ipv4, email, and onion, with 31 findings in total across them (endpoint 13, manifest 7, url 7, email 2, ipv4 1, onion 1). Read this as category presence and not as per-item recall: a category counts once it yields at least one finding, so this figure says frisk covers every planted category, not that it finds every planted item inside one. The six are named individually and the denominator is pinned by equality against the category list the gate grades. | benches/head-to-head/src/gate.rs test published_planted_ioc_category_bar_is_pinned_by_membership, which requires the published membership to equal the category list it grades so the row cannot describe a different six than the one measured |
| Pickle corpus (committed fixtures graded against the CPython pickletools reference) | pickletools-graded fixtures | 100.00% - 102 of the 102 pickle fixtures committed under corpus/pickle disassemble, trace and classify in agreement with CPython's own pickletools, which is the reference this figure is graded against rather than the pass reading its own output. The denominator is pinned by equality against the committed fixture set, so a run that inspects fewer fixtures scores worse instead of shrinking what it is measured against, and the figure is a named fixture set rather than a bare count, so one fixture cannot start failing while another starts passing. | crates/disrobe-pass-pickle/tests/corpus.rs test published_pickle_corpus_bar_is_pinned_to_the_graded_fixture_set, with the_pinned_corpus_check_rejects_a_dropped_fixture_and_a_shrunken_denominator refuting both weakenings; reference = the opcode and argument tables CPython's pickletools emits, committed beside the corpus |

The headline section is a filtered view of the full set; both are generated from the same committed JSON in one pass, so they cannot disagree.
