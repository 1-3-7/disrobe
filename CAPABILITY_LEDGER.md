# Capability ledger

One row per graded capability: the exact CLI command that exercises it and the
non-circular oracle that proves it. A capability earns a row only once a real
oracle (recompile-to-equivalence, a real interpreter or compiler, a real
competitor, or a ground-truth diff) grades it against something other than
disrobe's own output.

Numbers are the CI-enforced assertion floors (guaranteed reproducible lower
bounds), not best-case run counts. Figures marked [local] are self-reported on a
developer machine and are not enforced in CI. Every oracle skips rather than
falsely passes when its external toolchain is absent.

| Capability | CLI command | Oracle (non-circular) | Grading harness |
|---|---|---|---|
| Native x86-64 to C / to Rust pseudocode (in-tree, no external decompiler) | `disrobe native decompile <bin> --backend native --format c\|rust` | Execution-differential recompilation: each recovered function is recompiled by real gcc/clang/rustc and run against the original over an input battery (MS x64 + SysV ABIs); recover-or-sound-reject (unrecovered functions counted, never asserted wrong); teeth tests confirm the oracle flags a dropped call, swapped args, a corrupted loop bound, a neutralized guard, a flipped sign-extend. Floor: shape battery >=32/63 both ABIs, zero unsound. | `crates/disrobe-pass-native/tests/pseudo_c_leaf_oracle.rs`, `pseudo_c_wholeprog_oracle.rs`, `pseudo_rust_leaf_oracle.rs`, `pseudo_rust_wholeprog_oracle.rs` |
| JVM bytecode to Java source (classfile 1.0.2-25) | `disrobe jvm decompile <App.class\|app.jar> --out src/` | Recompile-equivalence (recompile-only): recovered .java compiled by real javac with per-method error attribution; reinforced by a real `java -Xverify:all` verifier gate and a stdout execution-differential. Floor: 131/131 top-level methods recompile error-free, GapCases 7/7, Hello multi-class 2/2. | `crates/disrobe-pass-jvm/tests/decompile_recompile_rate.rs`, `synchronized_labeled_recompile.rs`, `try_finally_recovery.rs` |
| .NET CIL to C#/F#/VB.NET + protector defeat (23 classified) | `disrobe dotnet decompile <in.dll>`; `disrobe dotnet deobfuscate\|peel <in.dll>` | Layered non-circular: recompile-to-IL-equivalence (recovered C# recompiled by csc, IL diffed via ilspycmd vs the original), MoveNext/user-method recompile-clean, Eazfuscator/KoiVM execution-differential (devirtualized CIL re-injected, stdout byte-compared on the real .NET runtime), ConfuserEx2 known-plaintext. Floors: 47 IL-identical methods, MoveNext 29, EazVM 5/5 + byte-identical stdout, KoiVM 6/6 bodies. Walls: ILProtector/Themida.NET/MaxToCode/ArmDot detect-only. | `crates/disrobe-pass-dotnet/tests/whole_type_il_equivalence_oracle.rs`, `movenext_recompile_oracle.rs`, `real_eazvm.rs`, `real_koivm.rs`, `confuserex2_gauntlet.rs` |
| Python .pyc to source (PyVersion 1.0-3.15) | `disrobe py decompile <in.pyc> --out <dir>` | Recompile-equivalence per code-object: recovered source recompiled by the real CPython interpreter, every nested code object compared via an opcode-normalized diff, a sibling-count guard blocking sibling-masking. Floors: pinned-corpus >=90% object-level (91% on 3.12), legacy 1.0-3.7 proven-correct >=150/191. Observed [local]: 3.14 stdlib 92.4% over 571 modules. | `crates/disrobe-pass-py-decompile/tests/arbitrary_recompile_gate*.rs`, `roundtrip_metric.rs`, `legacy_recompile.rs` |
| Pickle stream to re-executable source (never executes) | `disrobe pickle decompile <file> [--out out.py]` | Execution-differential: ~57 object graphs x protocols 0-5 pickled by CPython, disrobe reconstructs source, the harness exec()s it under a real CPython and deep-compares to `pickle.loads` of the same bytes; REDUCE is never called. Floor 100% (denominator excludes proven info-theoretic ceilings: protocol-5 out-of-band, EXT codes, persistent ids). | `crates/disrobe-pass-pickle/tests/roundtrip.rs`, `roundtrip_harness.py`, `corpus.rs` |
| PyArmor v6-v9/9-pro unwrap to original .pyc | `disrobe pyarmor unpack <wrapper.py> --out <dir> [--allow-bcc]` | Real-CPython behavioral-equivalence on the recovered .pyc plus identifier-vs-original ground truth (AES key extracted from the sibling pyarmor_runtime binary). Floor >=72/72 static free-mode samples, 6 named real-CPython behavioral cases. Walls: v3-v5 RSA-wrapped key (detect-only), BCC/super native bodies leaf-only. | `crates/disrobe-pass-pyarmor/tests/cpython_execution_oracle.rs`, `static_unpack_corpus.rs`, `v9_license_id_modes_real.rs` |
| Go binary recovery (pclntab/moduledata/types/embed/garble) | `disrobe go recover <bin> [--emit ...]`; `disrobe go info <bin>` | Real-toolchain-graded: names diffed against `go tool nm`, BuildInfo against `go version -m`, garble builds; byte-vs-original for flat-image and the UPX chain. Floors: func-name recall >=0.99 non-stripped / >=0.93 stripped, type-name >=0.85, garble stdlib >=0.85 (plain) / >=0.80 (-literals). Wall: seedless garble user-name hash (info-theoretic). | `crates/disrobe-pass-go/tests/go_realbinary_recovery.rs`, `go_buildinfo_oracle.rs`, `go_garble_gauntlet.rs`, `go_upx_flat_image.rs` |
| WASM to Rust / TS / WAT / C | `disrobe wasm decompile <in.wasm> --target rust\|ts\|wat\|c` | Execution-differential under real wasmtime (NaN-canonicalized, plus linear-memory-prefix compare); recovered Rust recompiled by rustc and re-run vs the original; op-coverage graded by real `wat` reassembly. Floors: >=76 funcs op-covered, >=40 whole-module and >=36 per-func execution-equivalent. | `crates/disrobe-pass-wasm-deob/tests/wholemodule_behavioral_oracle.rs`, `semantic_differential.rs`, `rust_execution_differential.rs` |
| PHP loader-chain deobfuscation | `disrobe php deobfuscate <in> --out <out>`; `disrobe php decode <in>` | Execution-differential: the obfuscated loader and disrobe's recovered source are both run through the real `php` binary and stdout is byte-compared; 12+ loader-idiom families. Walls: ionCube/SourceGuardian/Zend Guard per-file runtime-derived key (detect-only). | `crates/disrobe-pass-php/tests/php_runtime_differential.rs`, `php_real_chain_recovery.rs`, `peel_lossless_families.rs` |
| Dalvik/DEX + APK to Java source | `disrobe jvm decompile <in.apk\|.dex> --out src/` | Real JVM `-Xverify:all` verifier gate on every re-hosted body + source-fidelity vs the real EdgeCases.java + javac recompile. Floors: 102 verifier-clean classes with 0 lifter failures, >=307 re-hosted bodies clean, bodies_recovered >=353. Real-APK body-lowering ~89-92% [local, not CI]. | `crates/disrobe-pass-jvm/tests/dalvik_verifier_gate.rs`, `dalvik_decompile_oracle.rs`, `dex2jar_real_bodies.rs` |
| Native packer unpacking (27 detected; 10 implemented) | `disrobe native unpack <packed.exe> --out <bin>` | Byte-vs-original RVA-aligned section compare (blake3) where a disk-aligned reference exists, driven through an in-house x86 stub emulator; entropy-collapse + call-density execution proxy for ASPack/PECompact/MEW. CI-hard: UPX .text+.pdata byte-identical, whole-image >=96%; MPRESS .text >=90%; Yoda's Crypter blake3-identical. FSG/NSPack/Petite [local, fixtures not committed]. Walls: VMProtect/Themida native-VM. | `crates/disrobe-pass-native/tests/upx_unpack_all.rs`, `mpress_gauntlet.rs`, `packer_real_samples.rs` |
| frisk secrets / IOC recon (files/dirs/APK/archives/git) | `disrobe frisk <PATH> [--format text\|json\|sarif] [--git]` | Real-competitor head-to-head: disrobe and real apkleaks 2.6.3 run over the same APK bytes, both graded by recall against hand-verified planted secrets (ground truth = the planted tokens). Measured: disrobe 8/8 = 100% vs apkleaks 5/8 = 62.5%, floor 87.5%; 6/6 IOC categories, floor 100%. | `benches/head-to-head/src/frisk.rs`, `crates/disrobe-core/tests/frisk_gauntlet.rs`, `apk_frisk.rs` |
| NIR queryable IR (native + 9 bytecode ecosystems) | `disrobe query <input> "<expr>"` (functions \| calls-to \| xrefs-to \| string-decoders \| complexity-over \| capability) | Execution-differential: native x86 lifted to NIR, symbolically evaluated, asserted equal to concrete stub_emu execution across input vectors; cross-path fact-parity differential + real-binary ground truth (iced-x86-built ELF). Non-native lifters graded against hand-asserted facts. | `crates/disrobe-irsummary/tests/nir_symexec_oracle.rs`, `crates/disrobe-query/tests/nir_query_parity.rs`, `real_ir_oracle.rs` |
| VBA p-code to source (stomped / pcode-only macros) | `disrobe shell deob <in.docm\|.xlsm\|vbaProject.bin>` | Disassembly graded byte-for-byte against pcodedmp 1.2.6 golden dumps (external tool) + source lift graded against the authored .bas by token-overlap and identifier recall. Floors: 100% opcode+operand parity on 3 fixtures, SourceProbe >=90% line / >=95% identifier recovery. 264-opcode table, v5/v6/v7 32/64-bit. | `crates/disrobe-pass-shell/tests/vba_pcodedmp_oracle.rs`, `vba_semantic_source_oracle.rs`, `vba_pcode_lift.rs` |
| Native-language recovery (Nim / Zig / Crystal / D) | library `analyze()` (native-lang fingerprint reaches the CLI native path) | Real-demangler golden table (ldc2 / ddemangle) + independent-parser structure re-derivation over committed real binaries + cross-artifact stripped-PE-vs-.o grading. Floors: D demangle 30/30 vs ldc2, Crystal/D PE >=90% of the binary's own type/ClassInfo anchors, Nim/Zig .text line coverage >=80%. Wall: original source text (compiler-erased). | `crates/disrobe-pass-nativelang/tests/d_demangle_oracle.rs`, `oracle.rs`, `dwarf_dewall.rs` |
| Swift / Objective-C metadata + name recovery (Mach-O/.ipa/.swiftmodule) | `disrobe swift classdump <input>`; `disrobe macho classdump <input>` | Byte-for-byte vs the real Apple `swift-demangle` + names/types graded against ground-truth original source identifiers. Floors: symbol demangle >=95%, field-type >=98%, enum tuple payloads >=85%, reference demangler byte-parity >=75% (where swift-demangle is installed). Walls: native bodies not reconstructed, FairPlay cryptid runtime key. | `crates/disrobe-pass-swift-objc/tests/real_swift_demangle.rs`, `real_swift_typedump.rs`, `swiftmodule_dewall.rs` |
| Nuitka decompilation (onefile/standalone/module) | `disrobe nuitka decompile <exe\|build-dir> [--python out.py]` | Mixed non-circular: byte-vs-original onefile unpack (pinned BLAKE3), execution-differential for bodies recovered from Nuitka's generated C run vs the original on real CPython 3.14, and frozen-stdlib recompile-clean on on-box CPython. Floors: onefile 7/7 blake3, signatures 3/3, bodies 37/37 (fixtures present), frozen >=100 modules >=90% compile-clean. Wall: full native user-body decompilation. | `crates/disrobe-pass-nuitka/tests/binary_recovery.rs`, `widened_body_recovery.rs`, `frozen.rs` |
