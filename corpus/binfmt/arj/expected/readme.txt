ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

ARJ recovery corpus notes
=========================

This member exists so that the archive carries ordinary text as well as
engineered binary structure. The same plaintext is stored in every one of
the five archives, one per compression method, so a reader can compare the
recovered bytes across methods without depending on any decoder.

