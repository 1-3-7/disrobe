# DR-CLI-0082

**only --rung raw is implemented**

envelope create supports `--rung raw` only in v0.1.

## Common causes

- passed `--rung disasm` etc.

## Common fixes

- use `--rung raw` until other rungs land

## Source

Emitted from `crates/disrobe-cli/src/cli/envelope.rs`.

Look this up at runtime with `disrobe explain DR-CLI-0082`.
