# DR-CLI-0000

**subcommand not yet implemented**

the requested CLI subcommand exists in the parser but has no implementation in this build.

## Common causes

- running a v0.x build that still ships scaffolding stubs

## Common fixes

- check `disrobe --version` & `disrobe passes` to confirm which features are wired
- track progress at https://github.com/1-3-7/disrobe/issues

## Source

Emitted from `crates/disrobe-cli/src/cli/util.rs`.

Look this up at runtime with `disrobe explain DR-CLI-0000`.
