# DR-SDEF-0003

**sourcedefender empty filename**

the filename is empty, so the filename-derived password cannot be computed.

## Common causes

- empty path

## Common fixes

- pass a non-empty filename

## Source

Emitted from `crates/disrobe-pass-sourcedefender/src/error.rs`.

Look this up at runtime with `disrobe explain DR-SDEF-0003`.
