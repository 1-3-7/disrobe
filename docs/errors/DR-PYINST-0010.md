# DR-PYINST-0010

**PyInstaller bad pyver**

the pyver field did not decode to (major, minor).

## Common causes

- unknown PyInstaller bootloader fork

## Common fixes

- open an issue with the cookie hex dump

## Source

Emitted from `crates/disrobe-pass-pyinstaller/src/error.rs`.

Look this up at runtime with `disrobe explain DR-PYINST-0010`.
