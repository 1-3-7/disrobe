# Evidence report

Generated by `cargo run -p xtask -- evidence`. Every measured value below is read from its source: recovery-import rows from `xtask/data/recovery.json`, head-to-head and gate-harvest rows from `evidence/results/measured/*.json` (written by `cargo run -p disrobe-bench-head-to-head`). The report derives summaries from those records and validates declared paired counts, rates, and display text. Each row states the claim, the measured number, its evidence strength, the source or external oracle it relies on, and the exact command a stranger runs to reproduce it. `cargo run -p xtask -- evidence --check` is the CI drift gate that fails if any rendered number drifts from its source or any floor is violated.

33 evidence record(s) surfaced (30 CI-attested, 3 local), 2 head-to-head comparison(s) (`disrobe` trails on 0).

## Benchmarks

Oracle strength: `strong` = external-equivalence, execution, or byte-identity; `recompile-only` = the recovered source compiles but byte-equivalence is not asserted; `coverage-self-reported` = a coverage count graded against nothing external.

| ecosystem | claim | measured | strength | CI | evidence basis | reproduce |
|---|---|---|---|---|---|---|
| android | On their respective committed EdgeCases inputs, real javac scores every emitted method by one source-only rule. The scorer compiles the complete recovered source set first. If a parse failure blocks attribution, it isolates the implicated balanced method, field-initializer, or type region under a 64-round ceiling and reruns javac. Methods in isolated method or type regions are unclean. Peer methods count only after javac reaches attribution. A leg publishes a lead only when both sides are certified. | Android DEX: `disrobe` 56 / 163 (34.4%) vs JADX not certified (295 methods emitted; the producer exited nonzero); JVM classfile: `disrobe` 181 / 181 (100.0%) vs CFR 152 / 166 (91.6%) | recompile-only | [CI] | real javac (JDK), per-method recompile error-free against a STUBBED (empty) classpath so a wrong recovered signature cannot resolve against the original classes; one source-only scorer applies the same complete-compile, bounded region-isolation, and final diagnostic rule to every tool | `cargo run --locked -p disrobe-bench-head-to-head -- --check --only apk-jadx-cfr` |
| recon | apkleaks 2.6.3 recalls 5 of the 8 planted secrets in the committed planted-secrets.apk, measured by running the tool rather than by quoting its documentation. | 62.50% | strong | [CI] | the raw --json output apkleaks 2.6.3 wrote over this same committed apk, captured at evidence/competitors/apkleaks-2.6.3-planted-secrets.json and hash-pinned by the provenance record beside it, with its findings compared against the same planted token list | `cargo test -p disrobe-bench-head-to-head published_planted_apk_secret_bars_are_pinned_by_membership, defined in benches/head-to-head/src/frisk.rs` |
| beam | On 18 of 19 committed Erlang modules, disrobe's stripped-Dbgi Core Erlang lift emits source that recompiles with the original exports and returns the same test/0 result under Erlang/OTP 27.3.4. | 94.74% | strong | [CI] | real erlc and erl from Erlang/OTP 27.3.4: compile the original source, strip Dbgi and Docs, recover through disrobe's Core Erlang path, recompile, compare exports, then compare test/0 exit status and stdout | `DISROBE_REQUIRE_ERLANG=1 cargo test -p disrobe-pass-beam --test erlc_recompile_equivalence -- --nocapture` |
| binfmt | disrobe declares 100 container, archive, and firmware formats and carries an in-tree payload extractor for each, with no metadata-only or external-tool-gated formats. 33 of the 100 are driven to member bytes on disk by an input this repository commits. The other 67 have no committed input, so they are unverified rather than shown to fail. | 35 extracted from a committed input / 101 detected | coverage-self-reported | [CI] | the delivered count is disrobe extracting from its own committed corpus and counting the formats that wrote bytes, not an independent check. A member-byte diff against an external extractor is the proof this row does not yet have, and the 67 formats with no committed input carry no measurement at all | `cargo test -p disrobe-binfmt published_container_counts_match_this_enum` |
| android | disrobe lowers a Dalvik method body rather than a throw-stub for 82788 of 89516 defined methods across three real FOSS apks, and the real JVM verifier accepts 2960 of the 2994 recovered bodies that can be presented to it. | 92.60% | coverage-self-reported | [local] | two populations with different denominators: the 82788 of 89516 body-lowering count is the lifter counting its own output, and the 2960 of 2994 figure beside it is graded by real java -Xverify:all over bodies presented to the verifier, not over methods | `DISROBE_RUN_REAL_APK_TESTS=1 cargo test -p disrobe-pass-jvm --test dalvik_realworld_body_attest --test dex2jar_realworld_apks (local-only: the apks are gitignored)` |
| android | disrobe re-hosts Dalvik method bodies into class bytecode that the real JVM bytecode verifier accepts under -Xverify:all, on the committed dex corpus. | 100.00% | strong | [CI] | real JVM verifier (-Xverify:all over the assembled jar; the JVM rejects malformed bytecode) | `cargo test -p disrobe-pass-jvm --test dalvik_verifier_gate` |
| dotnet | disrobe decompiles a .NET CIL type to C# that recompiles error-free standalone under the real csc, across the EdgeCases corpus. | 51.43% | recompile-only | [CI] | real csc (dotnet SDK 9.0.316): recovered C# must recompile standalone as its own single-file compilation unit | `cargo test -p disrobe-pass-dotnet --test whole_type_il_equivalence_oracle edgecases_whole_type_recompile_fraction_is_published_as_measured -- --nocapture` |
| secrets | On the identical planted APK, disrobe frisk recalls more of the hand-verified planted secrets than apkleaks (it catches the AWS secret key, the Basic-auth credential, and the JWT that apkleaks's rule set misses). | disrobe 100.0% vs best competitor 62.5% | strong | [CI] | recall against the hand-verified planted ground-truth secret set committed in corpus/recon/apk/planted-secrets.apk; the planted secrets are the ground truth, so this is a non-circular recall oracle | `cargo run -p disrobe-bench-head-to-head  (apkleaks 2.6.3 via evidence/competitors/install-linux.sh; rule set pinned at evidence/competitors/apkleaks-regexes.json; needs jadx for apkleaks's decompile step)` |
| secrets | disrobe frisk detects every planted (non-secret) IOC category - endpoints, manifest findings, URLs, IPv4, email, .onion - on the committed planted ground-truth tree. | 6/6 (100.0%) | strong | [CI] | deliberately planted findings committed under corpus/recon/planted - the ground truth the frisk_gauntlet test asserts | `cargo test -p disrobe-core --test frisk_gauntlet  (harvested by cargo run -p disrobe-bench-head-to-head)` |
| recon | disrobe frisk recalls all 8 planted secrets in the committed planted-secrets.apk, and each of the 8 is named individually so one cannot lapse while another starts being found. | 100.00% | strong | [CI] | the planted token list committed alongside the apk, which is independent of what frisk reports; a scanner that misses a planted token fails regardless of what else it finds | `cargo test -p disrobe-bench-head-to-head published_planted_apk_secret_bars_are_pinned_by_membership` |
| recon | All 6 planted non-secret IOC categories are represented in frisk's scan of the committed planted tree, and the 6 are named individually so the published row cannot describe a different set than the one graded. | 100.00% | strong | [CI] | the committed category list the gate grades against, which is independent of frisk's output; the gate requires the published membership to equal that list exactly | `cargo test -p disrobe-bench-head-to-head published_planted_ioc_category_bar_is_pinned_by_membership` |
| go | disrobe recovers Go type names from a -s -w stripped binary by walking typelinks and moduledata, verified against the real go1.26.3 toolchain's own type metadata. | 100.00% | strong | [CI] | real go1.26.3 toolchain output (recovered type names compared against the toolchain's own typelinks/moduledata) | `cargo test -p disrobe-pass-go --test go_typemeta` |
| hermes | disrobe lifts every function of a real hermesc-built HBC v96 bundle with correct names and source-matching bodies at zero fallback ops. | 100.00% | strong | [CI] | self-authored sample.js compiled by the real hermesc; every function lifts at 0 fallback ops with source-matching bodies | `cargo test -p disrobe-pass-mobile --test real_hermes_sample` |
| jvm | disrobe decompiles JVM bytecode to Java whose observable per-method behavior under a real JVM matches the original, across the EdgeCases corpus. | 89.31% | strong | [CI] | real javac (JDK 25) plus a real JVM: the original and the recovered source are each compiled and run, and every observable per-method result is compared | `cargo test -p disrobe-pass-jvm --test edgecases_execution_differential` |
| jvm | disrobe decompiles JVM bytecode to Java that recompiles error-free under the real javac, per method, across the EdgeCases corpus. | 100.00% | recompile-only | [CI] | real javac (JDK 25): recovered Java must recompile error-free per method | `cargo test -p disrobe-pass-jvm --test decompile_recompile_rate` |
| lua | disrobe devirtualizes real IronBrew2 2.7.0 obfuscator output back to runnable Lua whose execution under the real Lua interpreter matches the original, in both standard and MAX mode. | 1 family | strong | [CI] | real Lua interpreter execution differential (recovered output executed under real Lua, asserted equal to the original) | `cargo test -p disrobe-pass-lua --test ironbrew2_real_oracle` |
| lua | disrobe lifts 86 of the 88 opcode entries in its declared Luau table; BREAK and NEWCLASSMEMBER are decoded and reported as unresolved. | 97.73% | coverage-self-reported | [CI] | none: the committed test enumerates disrobe's own declared opcode table and counts entries that avoid its unresolved-reporting path. No external semantic or upstream-format reference grades the result. | `cargo test -p disrobe-pass-lua --test published_luau_opcode_roster` |
| mba | across the 316 committed mixed-boolean-arithmetic cases, every answer disrobe returns agrees with the held-out original, and a case whose simplification exceeds a 2 second per-entry budget is named as a refusal rather than counted either way. A reference run answered 247 cases, refused 69, and an external bitvector solver proved 236 of the answers equal to the original while refuting none. | 316 corpus entries with a held-out original | coverage-self-reported | [CI] | the originals are held out of the input the pass receives. 100 of the 316 cases were produced by MBA-Obfuscator (ICICS 2021, MIT, pinned at commit 8574ef8) run over integer kernels this repository chose, so their obfuscated form is third-party and the original is the pre-transform kernel. The other 216 come from a forward rewriter under evidence/generators/mba that links no disrobe crate at all, which the workspace health check proves over the resolved dependency graph. Grading has two legs: a behavioural differential against check vectors the generator computed with its own evaluator, so a wrong answer cannot agree with a value the recovery path produced, and z3 or bitwuzla proving the recovered expression equal to the held-out original over every input, which is exact rather than sampled. | `cargo test -p disrobe-mba --test evidence_corpus_gate` |
| mcp | The MCP call_graph tool returns 5 direct-call edges for the committed stripped ELF, and all 5 match caller, callee, and classification evidence derived from its distinct committed unstripped toolchain twin. | 100.00% | strong | [CI] | caller and callee addresses from the committed unstripped toolchain twin, compared with the MCP server's stdio response for the stripped subject | `cargo test -p disrobe-mcp --test mcp_protocol_roundtrip navigation_tools_round_trip_a_committed_stripped_elf_over_stdio` |
| mcp | The MCP call_graph tool returns all 5 toolchain-derived direct-call edges for the committed stripped ELF. | 100.00% | strong | [CI] | caller and callee addresses from the committed unstripped toolchain twin, compared with the MCP server's stdio response for the stripped subject | `cargo test -p disrobe-mcp --test mcp_protocol_roundtrip navigation_tools_round_trip_a_committed_stripped_elf_over_stdio` |
| native | disrobe reaches 9 of the 9 states the dispatchers of the two committed ollvm-16 -fla functions can select, and reports a named reason for any state it does not reach rather than dropping it. | 9 reached by the recovered program / 9 states the two committed dispatchers can select | coverage-self-reported | [CI] | both sides of this ratio are disrobe's. The denominator is read out of the sample's own compare tree rather than typed into the gate, and a second pass re-derives it by scanning every equality compare against the state register in a linear decode of the whole function, so a tree walk that stopped early is caught instead of shrinking its own denominator. That makes the count harder to fake, not externally graded: no other tool supplies the state list. Behavior is graded separately and more narrowly, by executing the flattened bytes and the re-emitted recovered bytes under the in-house x86 interpreter and comparing the return value against corpus/native/ollvm/probe_src.c, the committed C source the fixtures were built from. | `cargo test -p disrobe-pass-native --test cff_dispatcher_cover` |
| native | disrobe unpacks PE packers (UPX, ASPack, PECompact, MEW, kkrunchy, Yoda's Crypter) through its own stub emulator and recovers section bytes that are byte-identical to the committed original, with no external tool participating. | byte-identity table, see results_md | strong | [CI] | byte-identity against the committed known-good original PE (RVA-aligned), plus stub-emulator round-trip equivalence | `cargo run -p disrobe-bench-native-unpack  (CI gates: cargo test -p disrobe-pass-native --test upx_unpack_all)` |
| pickle | disrobe disassembles every committed pickle fixture to a STOP, symbolically executes it, and classifies benign vs malicious, matching pickletools semantics across the whole corpus. | 102/102 (100.0%) | strong | [CI] | pickletools-semantics equivalence: every committed fixture must disassemble to a STOP, symbolically execute, and classify correctly (benign fixtures never flagged malicious, malicious fixtures always flagged) | `cargo test -p disrobe-pass-pickle --test corpus  (harvested by cargo run -p disrobe-bench-head-to-head)` |
| pickle | Every committed pickle fixture reconstructs to a value that re-executes equivalently under a real CPython interpreter, and a case walled as an information-theoretic ceiling is refused rather than counted toward the denominator. | 100.00% | strong | [CI] | real CPython: the reconstructed value is written back out and executed by the interpreter, and the result is compared with the original fixture's execution. The reference is the interpreter, never disrobe's own reconstruction. | `cargo test -p disrobe-pass-pickle --test roundtrip` |
| python | disrobe decompiles legacy CPython bytecode (1.0 through 3.7) to source that is proven correct by recompile-equivalence or, where the period interpreter is absent, a structural token-match against the original .py. | 78.50% | strong | [CI] | period CPython interpreters 1.0-3.7 (recompile-equivalence) or original .py source (structural token-match) | `cargo test -p disrobe-pass-py-decompile --test legacy_recompile` |
| python | disrobe recovers recompile-equivalent source for the majority of code objects across the entire CPython 3.14 standard library, measured over all 574 modules rather than a curated subset. | 95.09% | strong | [local] | CPython 3.14 (recompile the recovered source, compare emitted bytecode per code object) | `python crates/disrobe-pass-py-decompile/tests/harness/py_arbitrary_measure.py over the full CPython 3.14 Lib, the population crates/disrobe-pass-py-decompile/tests/full_stdlib_recompile_gate.rs measures under --ignored; CI does not run that gate and re-derives only the 115-module slice beside it` |
| python | disrobe recovers Python source whose recompiled bytecode is equivalent to the original, per code object, across the CPython 3.14 stdlib. | 96.60% | strong | [CI] | CPython 3.14 (recompile the recovered source, compare emitted bytecode per code object) | `cargo test -p disrobe-pass-py-decompile --test arbitrary_recompile_gate` |
| pyarmor | On 72 manifest-named PyArmor v8/v9 default-trial wrappers, disrobe statically decrypts each body and decodes one complete root CodeObject from its header-anchored marshal stream. | 72 decoded root CodeObjects / 72 named v8/v9 default-trial wrappers | coverage-self-reported | [CI] | none: the typed manifest and discovered v8/v9 wrapper set define the population; file identities, static decryption, and complete root-CodeObject parsing are checked in-tree. No external source, emitted pyc, execution, or tool comparison grades the result. | `cargo test -p disrobe-pass-pyarmor --test static_unpack_corpus` |
| ruby | disrobe decompiles Ruby YARV bytecode to source whose recompiled instruction multiset matches the original under the real MRI interpreter. | 98.67% | strong | [CI] | real MRI (ruby): recompile the recovered source, compare the YARV opcode multiset | `cargo test -p disrobe-pass-ruby --test yarv_recompile_oracle` |
| swift | disrobe recognizes and renders every Swift-mangled symbol in the committed SwiftHello Mach-O against pinned in-process output. No external demangler grades output correctness in this gate. | 100.00% | coverage-self-reported | [CI] | the committed SwiftHello Mach-O LC_SYMTAB fixes the named Swift-mangled symbol population; expected renderings are committed regression pins, not an external correctness oracle | `cargo test -p disrobe-pass-swift-objc --test swift_hello_symbol_pin` |
| taint | disrobe's taint engine recalls 93 of 190 labeled command-injection flows in the NIST SARD Juliet Test Suite's char/system/.c CWE-78 slice at gcc 16.1.0 -O2, with 0 false positives across every populated category, and the ground truth is the corpus's own manifest.xml and Flow Variant header, never a value authored in this repository. | 48.90% | strong | [local] | the NIST SARD Juliet Test Suite for C/C++ v1.3 (2017-10-01), CWE-78 OS Command Injection, char/system/.c files only. Ground truth is manifest.xml's own flaw line plus each file's own Flow Variant header, parsed by a typed reader that rejects an unrecognized line rather than defaulting. A finding is a true positive only when it names a function on the corpus's own bad-labeled call chain, and a false positive when it names one on the good-labeled chain; every group contributes exactly one bad-side and one good-side verdict, so a case cannot be silently dropped. | `cargo test -p disrobe-taint --test graded_corpus published_juliet_o2_bars_match_the_pinned_floors (unconditional, needs no corpus: compares xtask/data/recovery.json against the pinned floors below, and runs in CI); DISROBE_REQUIRE_JULIET_CORPUS=1 cargo test -p disrobe-taint --test graded_corpus juliet_cwe78_command_injection_precision_recall_o2 (re-derives the floor against the real corpus; local-only, since the corpus is fetched to a scratch cache by pinned URL and sha256, never committed, and CI does not provision it)` |
| wasm | disrobe lowers 1034 of the 1034 opcodes that wasm-tools counts across the 38 parseable modules of the committed corpus, and the WAT it emits re-assembles. | 100.00% | coverage-self-reported | [CI] | the denominator is external and pinned: wasm-tools 1.250.0 disassembles every committed .wat and its per-function instruction inventory is frozen under crates/disrobe-pass-wasm-deob/tests/golden/external_wasm_op_inventory.json, keyed by each fixture's BLAKE3. The numerator is still disrobe counting the opcodes it lowered, so this stays coverage rather than correctness; the emitted WAT is re-assembled by the wat crate before any of its opcodes count | `cargo test -p disrobe-pass-wasm-deob --test external_op_denominator --test semantic_recovery_corpus` |
| wasm | the execution-eligible functions of the committed WebAssembly corpus execute equivalently to the original under wasmtime after disrobe lifts and re-emits them. | 100.00% | strong | [CI] | wasmtime runs the original module and the recovered one on the same inputs and compares return values, trap parity, and the first 4096 bytes of linear memory, for the 57 execution-eligible functions | `cargo test -p disrobe-pass-wasm-deob --test semantic_differential --features sandbox` |

## Head-to-head comparisons

Within each declared leg, `disrobe` and the competing tool receive byte-identical input and the same external oracle. The `disrobe` row is bold. Losses are rendered in the same table as wins, never filtered. A declared comparison with a skipped or errored tool fails closed, never dropping the tool from its claim.

### APK / DEX decompilation: disrobe vs JADX vs CFR

On their respective committed EdgeCases inputs, real javac scores every emitted method by one source-only rule. The scorer compiles the complete recovered source set first. If a parse failure blocks attribution, it isolates the implicated balanced method, field-initializer, or type region under a 64-round ceiling and reruns javac. Methods in isolated method or type regions are unclean. Peer methods count only after javac reaches attribution. A leg publishes a lead only when both sides are certified.

comparison incomplete on this run (see tool statuses). CI-attested: [CI]

| tool | version | metric | result | status |
|---|---|---|---|---|
| **disrobe (in-house Dalvik, DEX input)** | n/a (in-process) | recompile-clean emitted methods (clean / emitted) | 56 clean / 163 emitted (34.4%) | ok |
| jadx (DEX input) | 1.5.5 | recompile-clean emitted methods (clean / emitted) | not certified: 295 methods emitted | uncertified |
| **disrobe (in-house JVM, JAR input)** | n/a (in-process) | recompile-clean emitted methods (clean / emitted) | 181 clean / 181 emitted (100.0%) | ok |
| cfr (JAR input) | CFR 0.152 | recompile-clean emitted methods (clean / emitted) | 152 clean / 166 emitted (91.6%) | ok |

Reproduce: `cargo run --locked -p disrobe-bench-head-to-head -- --check --only apk-jadx-cfr`

### Secret / IOC recall: disrobe frisk vs apkleaks

On the identical planted APK, disrobe frisk recalls more of the hand-verified planted secrets than apkleaks (it catches the AWS secret key, the Basic-auth credential, and the JWT that apkleaks's rule set misses).

`disrobe` leads or ties on this dataset. CI-attested: [CI]

| tool | version | metric | result | status |
|---|---|---|---|---|
| **disrobe frisk (in-process recon engine)** | n/a (in-process) | recall % | 8/8 (100.0%) | ok |
| apkleaks | v2.6.3 | recall % | 5/8 (62.5%) | ok |

Reproduce: `cargo run -p disrobe-bench-head-to-head  (apkleaks 2.6.3 via evidence/competitors/install-linux.sh; rule set pinned at evidence/competitors/apkleaks-regexes.json; needs jadx for apkleaks's decompile step)`

## Floors

Floors sit a declared margin below the measured value so a regression masked by an offsetting improvement is still caught; the harness records both.

| id | measured | floor | holds |
|---|---|---|---|
| apk-jadx-cfr | Android DEX: `disrobe` 56 / 163 (34.4%) vs JADX not certified (295 methods emitted; the producer exited nonzero); JVM classfile: `disrobe` 181 / 181 (100.0%) vs CFR 152 / 166 (91.6%) | n/a | yes |
| apkleaks-planted-apk-secret-recall | 62.50% | n/a | n/a |
| beam-erlang-recompile | 94.74% | 94.74 | yes |
| binfmt-extract | 35 extracted from a committed input / 101 detected | n/a | n/a |
| dalvik-realapk-coverage | 92.60% | 92.40 | yes |
| dalvik-verifier | 100.00% | 100.00 | yes |
| dotnet-whole-type-recompile | 51.43% | 51.43 | yes |
| frisk-apkleaks | disrobe 100.0% vs best competitor 62.5% | 87.50 | yes |
| frisk-planted | 6/6 (100.0%) | 100.00 | yes |
| frisk-planted-apk-secret-recall | 100.00% | n/a | n/a |
| frisk-planted-ioc-categories | 100.00% | n/a | n/a |
| go-typemeta | 100.00% | 100.00 | yes |
| hermes-opcoverage | 100.00% | 100.00 | yes |
| jvm-execution-differential | 89.31% | 89.31 | yes |
| jvm-javac-recompile | 100.00% | 100.00 | yes |
| lua-ironbrew | 1 family | n/a | n/a |
| lua-luau-opcode-coverage | 97.73% | n/a | n/a |
| mba-groundtruth-corpus | 316 corpus entries with a held-out original | n/a | n/a |
| mcp-direct-call-precision | 100.00% | 100.00 | yes |
| mcp-direct-call-recall | 100.00% | 100.00 | yes |
| native-cff-dispatcher-cover | 9 reached by the recovered program / 9 states the two committed dispatchers can select | n/a | n/a |
| native-packer-byterecovery | byte-identity table, see results_md | n/a | n/a |
| pickle-corpus | 102/102 (100.0%) | 100.00 | yes |
| pickle-roundtrip | 100.00% | 100.00 | yes |
| py-legacy-recompile | 78.50% | 78.50 | yes |
| py-stdlib-full | 95.09% | 95.00 | yes |
| py-stdlib-recompile | 96.60% | 96.60 | yes |
| pyarmor-corpus | 72 decoded root CodeObjects / 72 named v8/v9 default-trial wrappers | n/a | n/a |
| ruby-yarv-recompile | 98.67% | 98.67 | yes |
| swift-demangle | 100.00% | n/a | n/a |
| taint-juliet-cwe78 | 48.90% | 48.90 | yes |
| wasm-external-op-inventory | 100.00% | 100.00 | yes |
| wasm-wasmtime-diff | 100.00% | 100.00 | yes |
