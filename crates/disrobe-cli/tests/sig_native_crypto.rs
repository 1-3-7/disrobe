#![allow(clippy::expect_used, clippy::unwrap_used, clippy::panic)]
use std::path::PathBuf;
use std::process::{Command, Output};

use serde_json::Value;

const AES_SBOX: &[u8; 256] = &[
    ,
];

fn cli_binary() -> PathBuf {
    let exe: PathBuf = std::env::current_exe().expect("current exe");
    let mut dir: PathBuf = exe.parent().expect("exe dir").to_path_buf();
    while dir.file_name().and_then(|s| s.to_str()) != Some("debug")
        && dir.file_name().and_then(|s| s.to_str()) != Some("release")
    {
        if !dir.pop() {
            break;
        }
    }
    dir.push(if cfg!(windows) {
        "disrobe.exe"
    } else {
        "disrobe"
    });
    dir
}

fn temp_path(suffix: &str) -> (disrobe_core::scratch::ScratchDir, PathBuf) {
    let purpose: String = format!("disrobe-sig-native-{suffix}");
    let scratch: disrobe_core::scratch::ScratchDir =
        disrobe_core::scratch::ScratchDir::create(&purpose).expect("create scratch directory");
    let path: PathBuf = scratch.path().join("payload");
    (scratch, path)
}

#[test]
fn native_signatures_flags_embedded_aes_sbox() {
    let mut buf: Vec<u8> = vec![0u8; 64];
    buf.extend_from_slice(AES_SBOX);
    buf.extend_from_slice(&[0u8; 32]);

    let (_input_path_scratch, input_path): (disrobe_core::scratch::ScratchDir, PathBuf) =
        temp_path("bin");
    let (_out_json_scratch, out_json): (disrobe_core::scratch::ScratchDir, PathBuf) =
        temp_path("json");
    std::fs::write(&input_path, &buf).expect("write temp input");

    let out: Output = Command::new(cli_binary())
        .args(["native", "signatures", "--out"])
        .arg(&out_json)
        .arg(&input_path)
        .output()
        .expect("run disrobe native signatures");

    let report_bytes: Vec<u8> = std::fs::read(&out_json).expect("read signatures json");
    let _ = std::fs::remove_file(&input_path);
    let _ = std::fs::remove_file(&out_json);

    assert!(
        out.status.success(),
        "non-zero exit: {}",
        String::from_utf8_lossy(&out.stderr)
    );

    let report: Value = serde_json::from_slice(&report_bytes).expect("signatures json parses");
    assert_eq!(report["schema"], "disrobe.native.signatures/v1");
    assert_eq!(report["byte_count"], 352);
    assert!(
        report["crypto_constants"]
            .as_array()
            .expect("crypto_constants array")
            .iter()
            .any(|h: &Value| h["primitive"] == "aes-sbox"
                && h["offset"] == 64
                && h["confidence"] == "high"),
        "expected aes-sbox @ offset 64 with high confidence, got: {report}"
    );
    assert!(report["flirt_matches"].is_array());
    assert!(report["string_signatures"].is_array());
}
