rule:
  meta:
    name: tag-alpha and-or-not
    namespace: internal/example
    scope: function
    description: placeholder fixture combining and, or, not, mnemonic, operand
  features:
    - and:
        - or:
            - mnemonic: xor
            - operand:
                index: 1
                number: 42
        - not:
            - mnemonic: idiv
